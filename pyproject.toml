[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "customer-driven"
version = "0.1.0"
description = "Toolkit for text chunking and similarity analysis"
requires-python = ">=3.9"
dependencies = [
]

[tool.setuptools.packages.find]
where = ["src"]
